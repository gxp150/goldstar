Vue.compenent("greeting", {
    template: "<H1> Welcome to coligo!</H1>"
});

// create a new Vue instance and mount it to our div element above with the id of app
var vm = new Vue({
    al: "#app'"
});
